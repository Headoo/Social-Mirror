
// Orientation de l'appareil photo. 1 -> portrait. 0 -> paysage
apn_portrait=0;

// configuration SMTP email
smtp_port = 587;
smtp_server = headoo.net;
smtp_login = cacharelmirror@headoo.net;
smtp_password = cacaca;
// 1 -> ssl, 0 -> pas ssl
smtp_ssl = 0;

// configuration emplacement clavier. 1 -> en haut  0 -> en bas. 
keyboard_bottom = 0;